@model IEnumerable<Sistema_UVM_Control_Presupuestario.SOLICITUD>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CAPEX.DETALLE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OPEX.MONTO_NETO)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PERSONA_JURIDICA.NOMBRE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NUMERO_SOLICITUD)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DETALLE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CAPEX.DETALLE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OPEX.MONTO_NETO)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PERSONA_JURIDICA.NOMBRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NUMERO_SOLICITUD)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DETALLE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
